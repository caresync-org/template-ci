# =============================================================================
# ⚠️  NOT USED — No orchestrator in this setup.
#
# Each microservice workflow calls the 5 templates directly:
#
#   template-sonarqube.yml   ← Job 1: SonarQube analysis
#   template-build-snyk.yml  ← Job 2: npm install + Snyk scan
#   template-docker.yml      ← Job 3: Docker build + Trivy + push
#   template-approval.yml    ← Job 4: Manual approval gate
#   template-cd.yml          ← Job 5: Update argoCD-manifests repo
#
# This file is intentionally empty. Do not add workflow_call triggers here.
# =============================================================================
