# =============================================================================
# ⚠️  DEPRECATED — This file is no longer used.
#
# CareSync microservice workflows now call the 5 modular templates directly:
#
#   template-sonarqube.yml   ← Job 1: SonarQube analysis
#   template-build-snyk.yml  ← Job 2: npm install + Snyk scan
#   template-docker.yml      ← Job 3: Docker build + Trivy + push
#   template-approval.yml    ← Job 4: Manual approval gate
#   template-cd.yml          ← Job 5: Update argocd-manifests repo
#
# Each microservice workflow (auth.yml, doctor.yml, etc.) calls these
# 5 templates directly using 'uses:' with 'needs:' chaining.
#
# This file is kept for reference only and is NOT triggered by any workflow.
# =============================================================================
